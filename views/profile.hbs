<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Skate Park - Perfil</title>
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@4.6.0/dist/css/bootstrap.min.css" />
    <link rel="stylesheet" href="estilos.css" />
</head>

<body>
    <h1>Skate Park</h1>
    <div class="py-4">
        <h2>Perfil</h2>
        <hr class="w-50" />
        <div class="form-group row w-50 m-auto">
            <div class="form-group col-12 col-sm-6">
                <label>Email</label>
                <input type="email" value="{{skater.email}}" class="form-control m-auto" readonly />
            </div>
            <div class="form-group col-12 col-sm-6">
                <label>Nombre</label>
                <input type="text" value="{{skater.nombre}}" class="form-control m-auto" readonly />
            </div>
            <div class="form-group col-12 col-sm-6">
                <label>Años de experiencia</label>
                <input type="number" value="{{skater.anos_experiencia}}" class="form-control m-auto" readonly />
            </div>
            <div class="form-group col-12 col-sm-6">
                <label>Especialidad</label>
                <input type="text" value="{{skater.especialidad}}" class="form-control m-auto" readonly />
            </div>
            <div class="form-group col-12 col-sm-6">
                <label>Foto</label>
                <img src="{{skater.foto}}" alt="Foto de {{skater.nombre}}"
                    style="width: 60px; height: 60px; object-fit: cover;" />
            </div>
        </div>
    </div>
</body>

</html>